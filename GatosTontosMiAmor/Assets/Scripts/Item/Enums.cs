namespace Item
{
    public enum WeaponType
    {
        Sword,
        Bow
    }

    public enum ItemType
    {
        Weapon,
        ActiveAbility,
        PassiveAbility,
        HyperAbility
    }
}